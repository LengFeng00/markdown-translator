# Nginx 配置示例
# 将此文件放到 /etc/nginx/sites-available/markdown-translator
# 然后创建软链接到 /etc/nginx/sites-enabled/

server {
    listen 80;
    server_name translator.yourdomain.com;  # 修改为你的域名
    
    # 或者使用 IP 地址
    # server_name 123.456.789.0;
    
    # 项目根目录
    root /public/home/wangxueqiang/markdown-translator;
    index index.html;
    
    # 字符集
    charset utf-8;
    
    # 日志文件
    access_log /var/log/nginx/markdown-translator-access.log;
    error_log /var/log/nginx/markdown-translator-error.log;
    
    # 主要位置配置
    location / {
        try_files $uri $uri/ =404;
    }
    
    # 静态文件缓存
    location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
    
    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
    
    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/javascript application/json;
    
    # 安全头部
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}

# HTTPS 配置（使用 Let's Encrypt）
# server {
#     listen 443 ssl http2;
#     server_name translator.yourdomain.com;
#     
#     root /public/home/wangxueqiang/markdown-translator;
#     index index.html;
#     
#     # SSL 证书
#     ssl_certificate /etc/letsencrypt/live/translator.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/translator.yourdomain.com/privkey.pem;
#     
#     # SSL 配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     
#     # 其他配置同上...
# }
